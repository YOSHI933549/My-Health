{
  "name": "yoshi-health-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "YOSHI Health Tracker for web and iPhone",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "ios:sync": "npm run build:web && cap sync ios",
    "ios:open": "npm run ios:sync && cap open ios",
    "check": "node --check js/app.js && node --check js/sync.js && node --check js/n8n-sync.js && node --check js/ocr.js && node --check js/exercise-icons.js && node --check sw.js"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
